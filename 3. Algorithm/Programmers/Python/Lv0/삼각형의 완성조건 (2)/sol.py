# 가장 긴 변의 길이는 두 변의 길이의 합보다 작아야 한다.

def solution(sides):
    return 2 * min(sides) - 1

def solution(sides):
    return sum(sides) - (max(sides) - min(sides)) -1


# 결과적으로 이 함수는 "가장 긴 변"과 "나머지 두 변의 합" 사이에 대소 관계를 비교하게 됩니다. 
# 만약 나머지 두 변의 합이 가장 긴 한변보다 크다면, 이 함수는 양수 값을 반환하게 되고, 
# 그렇지 않으면 음수나 0을 반환하게 됩니다.

# 따라서, 이 함수가 양수 값을 반환할 때만 삼각형을 형성할 수 있습니다. 반
# 대로 음수나 0일 경우에는 삼각형 조건을 충족시키지 못합니다.

# 그러나 보통 이런 종류의 함수는 True/False 혹은 가능/불가능 등 구체적인 정보를 제공하는 것이 좋습니다. 
# 따라서 다음과 같이 수정할 수 있습니다: